# Strict Scope Rule

Stop making changes that were not explicitly asked for.
- Do not add unrequested UI components, buttons, navigation elements, or design elements.
- Execute only the precise, minimal modifications requested by the user.
- If an additional improvement seems relevant, propose it in text first rather than implementing it unasked.
